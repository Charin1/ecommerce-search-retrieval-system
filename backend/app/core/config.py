# This file is a placeholder for future configuration management.
# For a production app, you would use Pydantic's BaseSettings
# to load config from environment variables.

class Settings:
    PROJECT_NAME: str = "E-commerce Search API"

settings = Settings()